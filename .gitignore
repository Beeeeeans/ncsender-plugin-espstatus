# ESPHome build artifacts and secrets — never commit these
esphome/.esphome/
esphome/secrets.yaml
esphome/*.bin
esphome/*.elf

# Compiled firmware copied to plugin root for easy OTA upload
firmware.bin

# Plugin config (written by ncSender at runtime)
plugin-config/

# Node / editor cruft
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
